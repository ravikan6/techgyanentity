import { StudioCreateLayoutWrapper } from "@/components/studio/wrappers";

const StudioExpendedLayout = ({ children }) => {

    return (
        <StudioCreateLayoutWrapper >
            {children}
        </StudioCreateLayoutWrapper>
    )
}

export default StudioExpendedLayout;